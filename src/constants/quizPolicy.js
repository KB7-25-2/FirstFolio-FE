/** 퀴즈 포인트·재응시 정책 (미확정 — 설정값으로 분리) */

/** 정답 1문항당 지급 포인트 — 「포인트 = 정답 수」 */
export const POINTS_PER_CORRECT = 1

/** 동일 소단원 재응시 시 포인트 재지급 여부 */
export const ALLOW_DUPLICATE_POINT_GRANT = false

/** 시험지 UI 배점 표시용 (총점 = 정답 수 × 이 값) */
export const SCORE_PER_QUESTION = 10
